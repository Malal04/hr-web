export enum Role {
    Admin = 1,
    Manager = 2,
    Employee = 3
}

export enum AccountState {
    ACTIVE = 1,
    BLOCKED = 2,
    DELETED = 3,
} 

export enum DepartementStatus
{
    Active = 1,
    Archived = 2
}
